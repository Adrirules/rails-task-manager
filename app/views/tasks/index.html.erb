<% @tasks.each do |task| %>
<ul>
  <li> <%= check_box("tag", tag.id, {checked: task.completed}) %> <%= link_to task.title, task_path(task) %> <%= button_to "X", destroy_path(task.id), :method => "delete", data: { confirm: "Are you sure?" } %> </li>
</ul>


<% end %>

<%= link_to "+ add a new taks", new_path  %>
